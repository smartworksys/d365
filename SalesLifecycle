============================================================
Application Insights - KQL Queries for Presenting Requests
============================================================
Run in: Application Insights → Logs
Note: Sampling must be disabled in host.json for complete data.


------------------------------------------------------------
1. ALL REQUESTS (Full Detail)
------------------------------------------------------------

requests
| where timestamp > ago(30d)
| project
    timestamp,
    endpoint = name,
    httpStatus = resultCode,
    success,
    durationMs = round(duration, 0),
    userAgent = tostring(customDimensions["User-Agent"]),
    userId = coalesce(
        user_AuthenticatedId,
        user_Id,
        tostring(customDimensions["oid"]),
        tostring(customDimensions["userId"]),
        ""
    ),
    clientIP = client_IP,
    clientCity = client_City,
    clientState = client_StateOrProvince,
    operationId = operation_Id,
    customProps = customDimensions
| order by timestamp desc


------------------------------------------------------------
2. DISCOVER IDENTITY FIELDS (Run this first to see where user ID lives)
------------------------------------------------------------

requests
| where timestamp > ago(7d)
| take 50
| project
    timestamp,
    endpoint = name,
    user_Id,
    user_AuthenticatedId,
    user_AccountId,
    session_Id,
    client_IP,
    customDimensions
| where isnotempty(user_Id)
    or isnotempty(user_AuthenticatedId)
    or isnotempty(user_AccountId)


------------------------------------------------------------
3. USER ACTIVITY REPORT (Uses all possible identity fields)
------------------------------------------------------------

requests
| where timestamp > ago(30d)
| extend userIdentity = coalesce(
    user_AuthenticatedId,
    user_Id,
    tostring(customDimensions["oid"]),
    tostring(customDimensions["userId"]),
    tostring(customDimensions["x-ms-client-principal-id"]),
    client_IP
  )
| where isnotempty(userIdentity) and userIdentity != "0.0.0.0"
| summarize
    totalRequests = count(),
    firstSeen = min(timestamp),
    lastSeen = max(timestamp),
    distinctEndpoints = dcount(name),
    endpoints = make_set(name, 20),
    statusCodes = make_set(resultCode, 10),
    avgDuration = round(avg(duration), 0)
    by userIdentity
| order by lastSeen desc


------------------------------------------------------------
4. DAILY ACTIVE USERS (Counts by available identity)
------------------------------------------------------------

requests
| where timestamp > ago(30d)
| extend userIdentity = coalesce(
    user_AuthenticatedId,
    user_Id,
    tostring(customDimensions["oid"]),
    tostring(customDimensions["userId"]),
    client_IP
  )
| where isnotempty(userIdentity)
| summarize
    uniqueUsers = dcount(userIdentity),
    totalCalls = count(),
    avgDurationMs = round(avg(duration), 0),
    failedCalls = countif(toint(resultCode) >= 400)
    by Day = bin(timestamp, 1d)
| extend failRate = round(100.0 * failedCalls / totalCalls, 1)
| order by Day desc


------------------------------------------------------------
5. FAILED REQUESTS (401 / 403 / 500 with full context)
------------------------------------------------------------

requests
| where timestamp > ago(30d)
| where toint(resultCode) >= 400
| extend userIdentity = coalesce(
    user_AuthenticatedId,
    user_Id,
    tostring(customDimensions["oid"]),
    tostring(customDimensions["userId"]),
    client_IP
  )
| project
    timestamp,
    endpoint = name,
    httpStatus = resultCode,
    userIdentity,
    durationMs = round(duration, 0),
    clientIP = client_IP,
    operationId = operation_Id,
    customProps = customDimensions
| order by timestamp desc


------------------------------------------------------------
6. SPECIFIC USER LOOKUP (Try IP or user_Id - replace value)
------------------------------------------------------------

let targetUser = "PUT-USER-ID-OR-IP-HERE";
requests
| where timestamp > ago(30d)
| extend userIdentity = coalesce(
    user_AuthenticatedId,
    user_Id,
    tostring(customDimensions["oid"]),
    tostring(customDimensions["userId"]),
    client_IP
  )
| where userIdentity == targetUser
    or client_IP == targetUser
    or user_AuthenticatedId == targetUser
| project
    timestamp,
    endpoint = name,
    httpStatus = resultCode,
    durationMs = round(duration, 0),
    userIdentity,
    clientIP = client_IP,
    customProps = customDimensions
| order by timestamp desc


------------------------------------------------------------
7. EXPORT-FRIENDLY (For InfoSec Spreadsheet)
------------------------------------------------------------

requests
| where timestamp > ago(30d)
| extend userIdentity = coalesce(
    user_AuthenticatedId,
    user_Id,
    tostring(customDimensions["oid"]),
    tostring(customDimensions["userId"]),
    ""
  )
| project
    Date = format_datetime(timestamp, "yyyy-MM-dd"),
    Time = format_datetime(timestamp, "HH:mm:ss"),
    User = userIdentity,
    Endpoint = name,
    HTTP_Status = resultCode,
    Success = success,
    Duration_ms = round(duration, 0),
    Client_IP = client_IP,
    City = client_City,
    State = client_StateOrProvince,
    Operation_Id = operation_Id
| order by Date desc, Time desc


------------------------------------------------------------
8. REQUESTS + LINKED EXCEPTIONS (Correlate errors)
------------------------------------------------------------

requests
| where timestamp > ago(30d)
| join kind=leftouter (
    exceptions
    | where timestamp > ago(30d)
    | project
        operation_Id,
        exceptionType = type,
        exceptionMessage = outerMessage
  ) on operation_Id
| where isnotempty(exceptionType)
| project
    timestamp,
    endpoint = name,
    httpStatus = resultCode,
    exceptionType,
    exceptionMessage,
    clientIP = client_IP,
    durationMs = round(duration, 0)
| order by timestamp desc


------------------------------------------------------------
9. HOURLY TRAFFIC PATTERN (Spot anomalies)
------------------------------------------------------------

requests
| where timestamp > ago(7d)
| summarize
    callCount = count(),
    failCount = countif(toint(resultCode) >= 400),
    avgDuration = round(avg(duration), 0),
    p95Duration = round(percentile(duration, 95), 0)
    by bin(timestamp, 1h)
| extend failRate = round(100.0 * failCount / callCount, 1)
| order by timestamp desc


============================================================
NOTES:
------------------------------------------------------------
- Run Query #2 FIRST to discover which identity field has data.
- "coalesce" picks the first non-empty value from multiple fields.
- If no identity field has data, your Function code needs to log
  the oid from the token. Add this to your Function startup:

    var oid = req.HttpContext.User?.FindFirst("oid")?.Value
           ?? req.HttpContext.User?.FindFirst(
              "http://schemas.microsoft.com/identity/claims/objectidentifier"
              )?.Value;
    telemetryClient.Context.User.AuthenticatedUserId = oid;

  Or via Easy Auth header:
    var oid = req.Headers["X-MS-CLIENT-PRINCIPAL-ID"].FirstOrDefault();

- After adding oid logging, the user_AuthenticatedId field will
  populate and all queries will work automatically.
- Click "Export → CSV" after running any query.
- Adjust "ago(30d)" to change the time window.
- Ensure host.json has sampling disabled:
    "samplingSettings": { "isEnabled": false }
============================================================
