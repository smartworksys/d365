{
  "Premise Address": "@{items('Apply_to_each_CSV_JSON_entry')['premise_address']}",
  "Premise City": "@{items('Apply_to_each_CSV_JSON_entry')['premise_city']}",
  "Premise State": "@{items('Apply_to_each_CSV_JSON_entry')['premise_state']}",
  "Premise Zip": "@{items('Apply_to_each_CSV_JSON_entry')['premise_zip']}",
  "Primary Name": "@{items('Apply_to_each_CSV_JSON_entry')['primary_name']}",
  "Primary Address": "@{items('Apply_to_each_CSV_JSON_entry')['primary_address']}",
  "Primary City": "@{items('Apply_to_each_CSV_JSON_entry')['primary_city']}",
  "Primary State": "@{items('Apply_to_each_CSV_JSON_entry')['primary_state']}",
  "Primary Zip": "@{items('Apply_to_each_CSV_JSON_entry')['primary_zip']}",
  "Contractor Company Name": "@{items('Apply_to_each_CSV_JSON_entry')['contractor_company_name']}",
  "Contractor Address": "@{items('Apply_to_each_CSV_JSON_entry')['contractor_address']}",
  "Contractor City": "@{items('Apply_to_each_CSV_JSON_entry')['contractor_city']}",
  "Contractor State": "@{items('Apply_to_each_CSV_JSON_entry')['contractor_state']}",
  "Contractor Zip": "@{items('Apply_to_each_CSV_JSON_entry')['contractor_zip']}",
  "Distributor Invoice Number": "@{items('Apply_to_each_CSV_JSON_entry')['distributor_invoice_number']}",
  "Date Installed": "@{items('Apply_to_each_CSV_JSON_entry')['date_installed']}",
  "Measure Code": "@{items('Apply_to_each_CSV_JSON_entry')['measure_code']}",
  "Quantity": "@{items('Apply_to_each_CSV_JSON_entry')['quantity']}",
  "Product Name": "@{items('Apply_to_each_CSV_JSON_entry')['product_name']}",
  "Manufacturer": "@{items('Apply_to_each_CSV_JSON_entry')['manufacturer']}",
  "AHRI Ref Number": "@{items('Apply_to_each_CSV_JSON_entry')['ahri_ref_number']}",
  "Furnace Model Number": "@{items('Apply_to_each_CSV_JSON_entry')['furnace_model_number']}",
  "AC/HP Furnace Model Number": "@{items('Apply_to_each_CSV_JSON_entry')['ac_hp_furnace_model_number']}",
  "Furnace Serial Number": "@{items('Apply_to_each_CSV_JSON_entry')['furnace_serial_number']}",
  "Split System Coil Unit Model Number": "@{items('Apply_to_each_CSV_JSON_entry')['split_system_coil_unit_model_number']}",
  "Split System Coil Unit Serial Number": "@{items('Apply_to_each_CSV_JSON_entry')['split_system_coil_unit_serial_number']}",
  "Split System Condenser Unit Model Number": "@{items('Apply_to_each_CSV_JSON_entry')['split_system_condenser_unit_model_number']}",
  "Serial Number": "@{items('Apply_to_each_CSV_JSON_entry')['serial_number']}",
  "SEER": "@{items('Apply_to_each_CSV_JSON_entry')['seer']}",
  "EER": "@{items('Apply_to_each_CSV_JSON_entry')['eer']}",
  "AFUE": "@{items('Apply_to_each_CSV_JSON_entry')['afue']}",
  "Tons": "@{items('Apply_to_each_CSV_JSON_entry')['tons']}",
  "BTU": "@{items('Apply_to_each_CSV_JSON_entry')['btu']}",
  "Capacity": "@{items('Apply_to_each_CSV_JSON_entry')['capacity']}",
  "BTU Heat Furnace": "@{items('Apply_to_each_CSV_JSON_entry')['btu_heat_furnace']}",
  "BTU Heat Boiler": "@{items('Apply_to_each_CSV_JSON_entry')['btu_heat_boiler']}"
}
