Given that an application is registered in Microsoft Entra Identity with the necessary permissions, a function app is configured with logic to issue custom claims, and the TokenIssuanceStart event is set up in the Azure portal,
When a token is requested, triggering the custom function to evaluate and issue custom claims,
Then the generated token includes the expected custom claims accurately, and the issuance process completes without errors.
