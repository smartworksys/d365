Object.keys(localStorage).filter(function (k) {
  return k.indexOf("msal") >= 0 || k === "kofc_eapp_msal_cfg" || k === "biv_cfg" || k === "biv_result";
}).forEach(function (k) { localStorage.removeItem(k); });
console.log("MSAL + EApp cache cleared");
