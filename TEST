Object.keys(localStorage)
  .filter(function (k) {
    return k.indexOf('msal.') === 0 || k.indexOf('msal-') === 0
        || k === 'kofc_eapp_msal_cfg' || k === 'kofc_eapp_pending';
  })
  .forEach(function (k) { localStorage.removeItem(k); });
