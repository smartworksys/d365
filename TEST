if (ISoptionSetValue !== CONSTANTS.OPTION_SET_VALUES.PENDING) {
    if (!(ISoptionSetValue === CONSTANTS.OPTION_SET_VALUES.EXCEPTION && hasPBMRole)) {
        makeFormReadOnly(executionContext);
    }
}
