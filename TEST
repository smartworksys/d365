"Update_a_row": {
    "type": "ApiConnection",
    "inputs": {
        "host": {
            "connection": {
                "referenceName": "con-crm-ing"
            }
        },
        "method": "patch",
        "body": {
            "kofc_awdinstance": "@{variables('AWD')}"
        },
        "headers": {
            "prefer": "return=representation,odata.include-annotations=*",
            "accept": "application/json;odata.metadata=full",
            "organization": "@{parameters('CRMendpointUrl')}"
        },
        "path": "/api/data/v9.1/kofc_policyrequests(@{encodeURIComponent(triggerBody()?['kofc_policyrequestid'])})"
    },
    "runAfter": {
        "For_each": [
            "SUCCEEDED"
        ]
    }
}
