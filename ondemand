// Get the iframe containing the web resource
var iframe = document.getElementById("WebResource_yourWebResourceName");

// Access the dropdown inside the iframe
if (iframe && iframe.contentWindow) {
    var dropdown = iframe.contentWindow.document.getElementById("yourDropdownId");

    if (dropdown) {
        dropdown.innerHTML = ""; // Clear existing options
        // Add new options here
    }
}
