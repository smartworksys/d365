data.forEach(function(item) {
    if (item.type === "account") {  // Check if item.type is 'account'
        var option = document.createElement("option");
        option.text = item.name;
        option.value = item.id;
        dropdown.add(option);
    }
});
